interface:
  display_name: "Restaurant Data Capture"
  short_description: "Capture and update restaurant source packets"
  default_prompt: "Use $restaurant-data-capture to collect or update a restaurant source packet and emit one JSON file per restaurant."
